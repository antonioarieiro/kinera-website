version: '3.8'

services:
  mongodb:
    image: mongo
    restart: always
    container_name: kinergy-database
    environment:
      MONGO_INITDB_ROOT_USERNAME: kinergy
      MONGO_INITDB_ROOT_PASSWORD: kinergy
    ports:
      - "27017:27017"

  kine-website:
    build: .
    restart: always
    ports:
      - "3001:3001"
      - "30342:30342"
    depends_on:
      - mongodb